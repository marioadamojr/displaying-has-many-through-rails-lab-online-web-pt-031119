<h2><%= @patient.name %></h2> <br />
<p>
  With Doctor: <%= @patient.doctor %>  <br />
</p>
Appointments:
  <% @patient.appointments.each do |apt| %>
    <%= apt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> - <%= link_to apt.doctor.name, doctor_path(apt.doctor) %>
  <% end %>
